<%= simple_form_for(@schedule) do |f| %>

  <div class="form-inputs">
    <%= f.input :day, input_html: {class: 'span2'} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn-primary' %>
  </div>
<% end %>
